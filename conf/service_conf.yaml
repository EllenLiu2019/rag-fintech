milvus:
  host: https://in03-f663856b627ed8e.serverless.aws-eu-central-1.cloud.zilliz.com
  token: ${RAG_MILVUS_TOKEN}

postgresql:
  host: localhost
  port: 5432
  username: rag
  password: ${RAG_RDB_PASSWORD}
  database: rag_fintech
  pool_size: 5
  max_overflow: 10
  sslmode: disable
  
redis:
  host: 101.37.237.208
  port: 6379
  decode_responses: False
  password: ${ALI_REDIS_PASSWORD}
  queue_name: ingestion
  enable: True

neo4j:
  uri: neo4j://101.37.237.208:7687 
  username: neo4j
  password: ${NEO4J_PASSWORD}
  database: neo4j

storage:
  provider: oss
  bucket: rag-fintech
  endpoint: https://oss-cn-hangzhou.aliyuncs.com
  access_key_id: ${OSS_ACCESS_KEY_ID}
  access_key_secret: ${OSS_ACCESS_KEY_SECRET}
  prefix: rag-fintech

schema_path: rag/ingestion/extractor/schema/insurance.json

ingestion:
  extractor_model: qa_lite